export enum RoutingPages {
  home = "/",
  chapters = "/chapters",
  chat = "/chat",
  openSource = "/open-source",
  chatSettings = "/chat-settings",
  history = "/history",
  modes = "/modes",
}

export enum Views {
  viewMain = "view_main",
}

export enum Panels {
  home = "home",
  chapters = "chapters",
  chat = "chat",
  openSource = "open-source",
  chatSettings = "chat-settings",
  history = "history",
  modes = "modes",
}
